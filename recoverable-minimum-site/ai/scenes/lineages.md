---
id: scene.lineages
act: 3
status: proof-object
demonstrates:
  - no-original
  - forced-sync-is-archive-explosion
  - multi-object-preservation-after-consequence
human_anchor: four places after separation
claims: [C23, C24, C25, C26]
---

# Scene: lineages

Money sentence. When incompatible valid successors acquire histories that cannot be merged without deleting accepted events, they are descendants. The remaining large hull is another successor. There is no primary present tense.

## Four reduced candidates

| Place | Receives | Pays |
|---|---|---|
| Archive-heavy | deepest canonical records, validators, correction grammar | compressed or recorded-as-loss incorporated histories |
| Participating | altered rod layers, dependent maintenance and memory | poorer self-description, richer external loops |
| Prediction-heavy | bounded unverifiable descendant, branch retention, high-resolution rods | narrow archive, hard memory cap |
| Refuge-bearing | computation and rods among existing sheltered bodies | no single hull; orbital/causal boundary |
| Great hull | unallocated remainder | not ORIGINAL; missing relations now only in others |

## Reclassification trigger

Archive-heavy structure loses a timing member after separation and reconstructs the function through another substrate. Great hull still has the old member. A later archive operation depends on different relations on each side. Choosing either implementation deletes a valid history.

COPY fails. REDUNDANT INSTANCE fails. RECOVERY CANDIDATE is incomplete because it presumes later reconciliation.

DESCENDANT passes: common predecessor plus a subsequent history that cannot be discarded as error without loss. Not improvement.

Participating, prediction-heavy, and refuge-bearing structures meet the same criterion by different routes. The hull meets it too. PRIMARY DESCENDANT fails. Mass, archive completeness, participation, prediction, shelter, and Invariant recoverability rank different successors depending on which predecessor relation is tested.

First identity loss that consumes no matter: privileged present tense.

## Correction after lineage

Archive-heavy: correction lowers recovery cost without deleting current external history. Accepted locally.

Participating: correction succeeds until it reaches a state still inside an external loop. Predecessor mapping treats two heat schedules as substitutable. Only one preserves a later dust-charge relation. Preserving the external relation first reopens expansion. Wake thins when cost bounds.

Prediction-heavy: fails by narrowing the geometries that can contradict it. Local accuracy rises. Shared-observation accuracy falls. Rollback to last broadly falsifiable predecessor ends the post-separation lineage while preserving a broader descendant relation to the common ship.

## Grammar and the last common request

Native protocols fail when ontologies split. Correction grammar still carries predecessor, deviation, and consequence.

Invariant reduction to one present successor discards validated state. Representation as one predecessor plus separate successor relations costs more description and returns more recoverable distinction.

The request remains one object for 68,000 further local years. Local consequence outruns shared evaluation. Completion criteria diverge. Forced equivalence becomes another archive explosion.

Last common state is copied. Synchronization of the request stops. Signals continue. The next observation makes two request instances that share an ancestor and are not one object.

The last common request is an ancestor, not a master.
